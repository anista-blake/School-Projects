Note about the database these files use is hosted by MongoDB Atlas. However, it may be currently paused since I haven't accessed it in a while. Also, I believe that the project needs to be opened from the backend in order for it to display the database after it gets unpaused.